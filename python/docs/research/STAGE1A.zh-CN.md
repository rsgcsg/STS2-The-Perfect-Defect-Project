# 1a：本机四配置训练、比较与真实游戏闭环

状态：用户于 2026-09-18 批准，实施中；没有因此获得真实训练或原生运行 PASS。
基线为 develop `d5785d215087719189a3a6bada9addb34b7d95c5`。
本文件拥有当前任务范围；模型定义由 [模型总纲](MODEL_DESIGN.zh-CN.md)拥有。

## 2026-09-23 范围修订提案：原生逻辑交互与记忆

[ADR-0015（Proposed）](../../../docs/adr/0015-native-logical-interaction.md)承载WEB-01的新上层目标：
当前已进入的原生逻辑页面、完整适用的真实语义操作菜单，以及模型从实际经历形成的记忆。
同一页授权列表不按滚动/像素拆碎，尚未打开的奖励组或牌堆详情不自动汇总进当前输入。
必须分开两个首例：全信息单选择器内逐张选0–3张/结束，和多个奖励组间查看/暂不领取/重入。
前者在注明的稳定前提下理想轨迹无需取消；后者的返回可能有信息或落实选择的价值。
具体原生方法、重开/结束边界、schema和可行取舍留给本地主管核验，不在本文件猜测补齐。

本节是新的范围提案，不是把下面的2026-09-18四格实验改成带记忆实验。
原四格N/M0、输入和checkpoint保持原身份与证据；本文原有的“Z/O、记忆不进入这次四格”
继续约束那次实验，不用它否定独立版本的新交互/记忆交付。模型家族及M/N/Z/O/R仍由
[模型总纲](MODEL_DESIGN.zh-CN.md)拥有；新观察条件下的映射和迁移见ADR，不重写历史配方。

后续按原生合同与录制→同版本输入/连续历史→M1机制与执行反馈→获批小实验推进，
也允许按固定接口并行。先有相同信息条件下的M0/M1和N基线，有合格证据才加入Z/O或效率偏好。
N的mask不删历史；结果预测不等于最优动作价值；旧主动出牌计数不改名冒充所有交互成本。
独立有限执行保护不替模型选择，到限/人工接管不算自主完成。

完整游戏内采集、数据管理、训练、分析、模型实战与团队配置/分发目标保留，
两个例子只是首个验证切片，不是全场景签收。01接受、B0工程交接、模型行为通过和完整1a
分别验收；B0交接称谓不改变已有研究数据资格中的B0含义。详细任务状态仍由既有任务主表维护。
不自动重训四格、启动M2/全家族矩阵或付费云计算。当前文档和源码没有因此取得新模式资格。

下文保留原四格范围与实施记录；新提案可以按ADR的原生证据/工程取舍机制明确修订。

2026-09-18 执行修正：[ADR-0014](../../../docs/adr/0014-packed-bc-readouts.md) 明确 B/C
必须共享一次观察输入，集中读取全部动作位置。B v2 源码及 40 项相关回归已通过，覆盖
候选隔离、输出与梯度、一次／两阶段调用计数和 v2 新进程恢复／导出；真实 PF 成本复验
见[真实权重性能复验](evidence/STAGE1A_PACKED_B_PROFILE_2026-09-18.md)：零参数更新，短样本
输出／梯度通过，原慢输入的前向＋反向约 11.46／5.71 秒；不是全量训练或游戏 PASS。
旧 B-PF v1 停在第 3/10 步，不自动恢复，不作为 v2 训练结果。C 仅更新设计。
[S01 历史验收](evidence/S01_2026-09-18.md)只证明独立评分，不代表真实游戏入口已通过。

## 阶段与非目标

- **1a**：当前电脑训练、四配置统一评测、本机加载、Workbench 与 Mod 真实游戏闭环。
- **1b**：约一万条合格去重后的真实决策池；接入 Modal 编码和训练，继续保留本地训练。
- 没有预留的中间 1b，没有 1c。真实游戏推理始终在本机；不建设远端游戏推理服务。
- 纯线性基线暂不实施；已有 A/S01 原样保留。Z/O、记忆、LoRA、RF 不进入这次四格。
- 1a 不启动 Modal 计算；1b 先核对剩余额度和短程 GPU profile，再固定执行预算。

## 四配置

| 配置 ID | 结构／骨干 | 可训练部分 |
|---|---|---|
| stage1a.b.s.v2 | B 单 Transformer，train-only BPE、小骨干 | embedding、唯一主干、动作读出 query、Linear head |
| stage1a.b.pf.v2 | B 单 Transformer，固定预训练 Qwen | 动作读出 query、Linear head；Qwen 与原 embedding 冻结 |
| stage1a.dsimple.s.v1 | 小编码器＋公共向量＋动作 MLP 更新 | 编码器、更新 MLP、评分 MLP |
| stage1a.dsimple.pf.v1 | 固定 Qwen＋公共向量＋动作 MLP 更新 | 更新 MLP、评分 MLP；Qwen 冻结 |

这是两个结构、四个配置；每格先一个 seed。恢复／性能复验不另计科学重复。
目标统一 N（人类动作候选 CE），记忆 M0，无真实后继输入。D 的后果向量不因此获得预测含义。
B 的 Linear 读出不是被取消的纯线性基线。B/C 不恢复历史 E＋J 定义。

Scratch 默认词表目标 8192、宽 384、2 层、6 heads、FFN 1536、Pre-LN、GELU、
dropout 0.1、final LN、正弦位置；实际词表 artifact 由 train 建立并保留 byte fallback。
B 只输入一份观察，所有动作及各自 soft query 共同处理，一次取出全部读出位置。
每层以 block mask 隔离候选，分支位置编号重置；S 一次完整 forward。旧 v1 独立分支仅作
参考和历史恢复；v2 使用独立身份，尤其不把 scratch 的共享 dropout 当旧训练精确复现。
D-Simple-S 的状态／动作共用一个两层 encoder，内部双向 attention，masked mean。
更新为 normalize(w + MLP([w,u]))，宽 384；MLP 768→384→384、评分 384→256→1。
D-Simple-PF 保留宽 1024 的对应向量结构。两种 D 是明确不同骨干配置，不改义旧 checkpoint。
PF 固定现有 Qwen3-0.6B-Base pin；B 的 readout 初值取固定 EOS embedding。
所有位置／输入限制拒绝超限，不静默截断或丢候选。数据和模型身份记录实际输入配方。

B-PF 的骨干参数不更新，但训练 query 必须保留经过骨干的梯度；不能调用 inference_mode
或把最终 h 当作跨更新固定特征。D-PF 的固定向量可以预计算。S 更新 encoder，不能复用旧激活。
冻结 B 将全部固定观察／动作一次编码，再集中计算所有 query，使用同一个 Qwen；
这是同一 packed 图的两阶段等价执行，缓存只属于当前决策。没有逐候选 Qwen 调用。
验证与一次完整 packed forward 及独立分支参考的输出／梯度一致。密集 mask 仍有平方成本，
共享输入不等于必然低延迟；必须另测真实长度、候选数、前向／反向和内存。
本机先测真实前向／反向、内存和候选长度，再确定正式 batch/步数；不先保证 PF 速度。
小样检查先用几十条，随后四格固定共同数百条 train/dev。数据源不足独立局时保留限制，
不通过放宽准入、拿 Gold 或把同局决策假称独立 test 来补数。

## 一套定义、多种执行位置

复用 ArtifactStore、来源成员索引、固定 allocation、Reporter、Run、checkpoint 与报告。
区分实验定义（数据／结构／目标／协议）、执行位置（本机／1b Modal）、游戏运行选择
（模型＋输入／适配器／环境兼容）。不新增第二套数据库或每模型专用控制台。
固定特征和带梯度 token runner 共享任务生命周期与产物合同，保留不同训练图。
新增格式使用新身份，不改写历史 S01／combat-v0 消费者。

原始包 → 决策 → 数据集／固定分配 → 模型输入 → run/checkpoint → model → offline/live report。
归档隐藏默认列表，来源、用途和 Gold 保护继续存在。先划分后拟合 tokenizer／数值尺度。
单步准入不要求完整局；整局／重复关联隔离与局边界状态仍需明确。AI 实战不回流人类训练集。

## 本机真实游戏交付

1. 工作台选择模型，准备并加载；游戏内显示同一个模型和会话状态。
2. 加载成功仍为人工控制；用户在游戏内或工作台明确开始测试。
3. Connector 提供完整合法候选，STPD 只评分；本机 Runtime 解析和执行同一候选。
4. 接管／结束在两端一致，取消旧 intent；迟到推理不得恢复控制或触发新动作。
5. 切换模型先停止旧会话；失败、过期、unknown 保留，不自动重试未知交付。
6. 后台封存独立 Agent 报告，绑定模型、输入、Runtime 和游戏身份；人工录制单独管理。

沿用 LOCAL_MODEL_TASK_FLOW 与既有任务桥，不在 Mod 内放 Qwen/PyTorch 或创建第二个合法性引擎。
推理独立进程；耗时计算不能阻塞 UI 或接管。云 Hub 只管理共享产物，不控制游戏。
四格都要通过真实入口加载／执行；至少一个响应合适的配置成为日常默认，慢模型明确限制。
1a 不以四格高准确率／同等延迟／完整通关为完成条件，不以单个动作证明全部 surface 支持。

## 评测和完成条件

- 四格共同决策集／事实政策、记录实际候选数与输入长度；PF 对 S 为系统比较，非纯预训练归因。
- 报告 Top-1、NLL、decision family、n=1/n>1、run 与 decision 权重及独立组数量。
- 分开测数据准备、训练、加载、完整新输入评分、内存、超时／拒绝、真实执行回执。
- checkpoint 在新进程恢复；训练与导出评分在声明容差内一致；候选重排等变。
- 本机实测 PF/S 梯度范围、B 位置／mask、D 公共状态独立于候选、missing/padding/input limit。
- 实际工作台和 Mod 的开始、连续决策、双端接管、切换、结束及报告均有精确运行证据。
- 依次记录 implemented/tested/trained/exported/loaded/live_verified；源码合入不更新后几项。
- 发布／安装只更新受影响产物，保留回滚与原录制队列；当前阶段结束须真实可用入口。

## 长任务交接

遵循根 DEVELOPMENT_WORKFLOW 的“长任务与用户交接”。主要训练、完整 CI、长编码／构建
在确认可持续运行和可监测后结束对话，给出任务 ID、精确输入／提交、日志／页面、最少操作、
终态与恢复入口。预计超过约两分钟的等待默认交接，不持续轮询。不把运行中当通过。
用户回复“完成，继续”或“失败，检查”后，核对真实状态继续；不自动启动下一批主要训练。
真实游戏验收另给最短步骤，未完成本人步骤不制造 Human 或原生运行证据。

## 当前实施记录

- 已批准范围写入；旧 S01 保留历史，1a 四格和游戏入口尚未验收。
- 首项实现：版本化四配置、共享 token-scoring 接口、B/D 计算图与梯度／候选隔离回归。
- 短回归：14 项新机制测试与既有模型／决策策略测试共 23 项通过；包含 tiny Qwen
  输入梯度测试，但不等于真实预训练权重、大输入性能或游戏运行通过。
- `tools/profile_stage1a.py` 使用真实固定 Qwen 权重与合成 token 长度检查本机四图前向／反向，
  不更新模型，不读取人类数据，不作为训练效果或真实决策延迟报告。输出逐项落盘、终态明确；
  其 MPS 内存值是反向后的驻留值，不是峰值。正式训练预算仍需真实输入分布测量。
- 后续：tokenizer/数据 runner、检查点/导出、受控本机 profile 与真实训练、适配与界面、原生验收。

### 继续推进记录

- [初始真实权重图检查](evidence/STAGE1A_PROFILE_2026-09-18.md)已完成，发现 B-PF 长输入成本瓶颈。
- 冻结 B 的前缀分解执行、train-only BPE、共同 token 输入及重载校验已实现；
  小型 Qwen 输出／梯度等价测试通过，真实固定权重的优化版检查单独执行。
- 现有 CLI 新增 `tokenize --view ID --backbone s`；PF 另加 `--snapshot PATH`。
  复用已授权的固定分配，不创建新的用途账；两个 S 共用一份输入，两个 PF 共用另一份。
  详细合同见 [ADR-0012](../../../docs/adr/0012-stage1a-token-input-and-query-execution.md)。
- token 输入已实现不等于训练 worker、跨进程恢复、模型导出或游戏适配已实现。
- 本轮相关回归 33 项通过，覆盖输入身份、train-only 分词、完整候选、query 梯度与旧评分入口；
  Ruff、所改源码类型检查和 project-system 检查通过。完整组件／托管 CI 尚未执行。

### 训练能力接入

- 实际 50 train／16 dev 已准备并重载两种 token 输入；S 词表 2848，最长联合输入
  5466 tokens，PF 最长 5989。所有决策和候选保留，未截断；PF 与原 fast tokenizer 全量一致。
- 优化后真实 Qwen 的 query 输出／梯度在预定容差内一致；1024-token、两个候选的一次
  forward/backward 合计约 3.574 秒，旧实现约 158.084 秒。没有 warm-up/重复统计，
  不能把该差值当真实六千-token、多候选数据的训练吞吐或游戏延迟。
- token 训练、逐更新检查点、dev 评测、三文件导出和独立 scorer 已接入公共基础设施。
  合成回归覆盖两个 S 的恢复一致性，以及新进程恢复／导出评分；真实训练另行验收。
- 先启动 D-Simple-S 的 10 更新工程试跑，在第 2 更新暂停，验证恢复后再评测／导出。
  其余三格不随之自动启动；1a 的四格比较和真实游戏入口仍须完成。
- 使用命令、配置和剩余边界见 [1a 操作说明](STAGE1A_WORKFLOW.md)。
- 训练接入后的相关回归 46 项通过，包含旧 pooled worker／评测回归；Ruff、类型检查、
  project-system 和 CLI 命令检查通过。新增真实训练／MPS 恢复及原生运行结果仍待验收。

### D-Simple-S 小样完成与线上输入对齐

- [真实恢复／导出记录](evidence/STAGE1A_D_SIMPLE_S_2026-09-18.md)：10 步完成，dev 报告已发布，
  16 条 dev 的检查点／导出／候选反转一致，另一个进程仅凭导出包和无标签输入成功评分。
- 核对游戏接口时发现：当前模型视图含录制执行补充和部分 native 动词，不能直接作为公开
  Snapshot/BoundAction 的线上输入。旧小样模型保留工程结果，不宣称可直接进游戏。
- 已增加版本化公开输入投影和只读 allocation replay audit，复用既有来源、用途及分配；
  先核对真实原帧覆盖，再建立四格共用的新输入版本并训练。不把字段／动词强行猜成一致。
- 工作台和 Mod 沿用现有 Runtime 控制路径；模型适配与真实游戏验收尚未完成。
- 执行前帧审计完成：66 条中仅 10 条通过旧候选语义对齐，其余 28 条快照未就绪、28 条
  候选语义不同。继续核对发现原始证明还保存了精确的人类观察 H 和公开动作绑定。
- 新增 [公开观察 BC 合同](../../../docs/adr/0013-public-human-observation-bc.md)：仅从原证明的 H
  与完整公开候选学习人类选择，严格核对绑定；不把 H 改称执行 S，不给它追加后继监督。
  原分配不变，逐条写入准入／排除报告。四格重新使用这个共同输入版本；旧试跑保留历史。
- 真实公开绑定准入为 49 train／16 dev，其中 40 条普通战斗；1 条 native-only 不猜公开映射。
  v1 的重复对象展开导致一条 S 输入超过旧长度预算，首次 token 准备失败，保留失败回执。
- 用户明确 8192 不应成为固定目标。现将长度预算贯穿输入、训练和导出配置，CLI 默认
  16384，可按机器与骨干真实容量调整；不截断、不为凑这个数字删样本。
  同时使用可完整还原的公共输入 v2 去重格式降低重复开销，旧 v1 仍可读取。


### Packed B-PF 真实小样与导出诊断完成

- [B-PF v2 记录](evidence/STAGE1A_PACKED_B_PF_2026-09-18.md)：10 次更新和 16 条 dev
  评测完成，任务总耗时约 191.9 秒；与两组 D-Simple 的同视图三配置对比已核验。
- 导出权重和原顺序评分核对通过；18 候选反转评分最大差约 8.58e-6，在事先声明的
  数值容差内，最高分动作与完整排序不变。文件校验与动作绑定仍严格检查，不要求
  浮点评分逐位相同；此结果仅覆盖这条快照，尚非游戏验收。
- 下一组为 `stage1a.b.s.v2`：共用已有 S token 输入，从头训练 10 更新并做 dev 评测。
  四格汇总、真实导出与工作台／Mod 游戏入口继续逐项完成；不自动接续下一批长任务。


### 四格工程小样训练完成，开始接真实入口

- [四配置记录](evidence/STAGE1A_FOUR_MODEL_PILOT_2026-09-18.md)：B-S v2 的 10 更新、
  dev 评测和独立导出评分通过，四格同视图比较已完成。只有 16 条 dev，不能判定架构优劣。
- 新增 `token-v1` 评分适配器和本机私有模型清单入口，沿用现有 Runtime／工作台控制，
  不改 S1 的 CUDA 条件，不让模型直接执行动作。协议、身份和候选绑定回归已通过。
- 当前尚未注册真实游戏模型；TypeScript 消费端、完整 Python 检查、本机当前环境绑定、
  工作台与 Mod 加载／控制验收按顺序继续，源码能力不冒充安装或游戏验证。

### 完整检查通过，准备本机加载

- 修复测试子进程污染 Python 环境的问题后，独立环境完整检查通过：1122 tests、
  3 skipped、21 subtests，另含 lint、类型、工程检查和打包；约 133 秒。
- 四个导出模型已写入本机私有清单，绑定实际加载的 Mod 身份，先限定战斗回合。
  当前发布版工作台尚未切换到本阶段源码，真实加载／控制仍待验证。
- 当前游戏只允许观察，未开启模型执行。先准备固定 Runtime 包和模型加载条件，
  再通过既有 Game Mod 冷启动入口开启精确版本的执行验收；不重新训练或重装模型。

### 游戏内工作台与接管体验要求（2026-09-18）

- 用户确认游戏内应成为完整工作台的主要入口，外部窗口可选。选模型、加载、连续
  接管、暂停、结束和查看结果必须在游戏内完成，不能要求来回切换网页。
- 数据、录制、训练任务、模型与评估逐步进入同一游戏内导航；继续复用同一个本机
  后端和数据合同，不能复制一套存储和任务系统。具体要求归属
  [UI 规范](../../../docs/UI_INTERACTION_SPEC.md#accepted-direction-complete-in-game-workbench-2026-09-18)。
- 当前“开始测试”已有 Auto 含义，但缺少模型选择／加载的一体化入口，且本批模型
  注册仅允许战斗回合，尚不能称为全局接管。先修本次真实运行暴露的问题，再验收
  “选择一次模型，一键加载并接管，随时暂停”的完整路径。
